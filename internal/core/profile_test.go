package core

/*
func TestProfile(t *testing.T) {
	tests := []struct {
		name    string
		value   int
		wantErr bool
	}{

		{
			name: "проверка отсутствия конфигурации профиля", value: noneProfile,
		},
		{
			name: "проверка  профилирования в файл base", value: baseProfile,
		},
		{
			name: "проверка  профилирования в файл result", value: resultProfile,
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			server, err := Config()
			require.NoError(t, err)
			err = profile(server.ProfileType)
			require.NoError(t, err)
		})
	}
}
*/
